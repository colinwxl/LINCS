import React from 'react';
import styles from './AppsView.scss';

export default function ExpWorkflows() {
  return (
    <div className={`${styles.workflow} ${styles['workflow-exp']}`}>
      <h5>Experimentalist</h5>
    </div>
  );
}
